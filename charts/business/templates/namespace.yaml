apiVersion: v1
kind: Namespace
metadata:
  name: {{ .Values.namespace }}
  labels:
    {{- include "business.labels" . | nindent 4 }}
    # Pod Security Admission: enforce the strictest profile.
    # All workloads in this chart already comply (runAsNonRoot, no caps,
    # readOnlyRootFilesystem, seccompProfile=RuntimeDefault, etc).
    pod-security.kubernetes.io/enforce: restricted
    pod-security.kubernetes.io/enforce-version: latest
    pod-security.kubernetes.io/audit: restricted
    pod-security.kubernetes.io/audit-version: latest
    pod-security.kubernetes.io/warn: restricted
    pod-security.kubernetes.io/warn-version: latest
